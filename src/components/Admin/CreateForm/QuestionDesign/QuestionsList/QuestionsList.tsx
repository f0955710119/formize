import { FC } from "react";
import styled from "styled-components";
import Layout from "../UI/Layout";

const ListLayout = styled(Layout)`
  width: 22%;
`;

const QuestionsList: FC = () => {
  return <ListLayout>1</ListLayout>;
};

export default QuestionsList;
